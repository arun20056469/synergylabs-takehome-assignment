# Integrations guide

AuroraDesk supports Slack, Microsoft Teams, and a REST API. The REST API uses bearer tokens and has a default rate limit of 600 requests per minute per workspace. Webhooks can notify a customer endpoint when a ticket is created or updated.

API tokens should be rotated every 90 days. Administrators can revoke tokens immediately from the workspace security page.
