services:
  - type: web
    name: synergylabs-rag
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn problem1_cost_efficient_rag.src.api:app --host 0.0.0.0 --port $PORT